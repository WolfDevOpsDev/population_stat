# 🌍 Population Dashboard (Docker + Streamlit + PostgreSQL)

Цей проект є веб-застосунком для візуалізації та аналізу демографічних даних країн світу. Він автоматизує процес збору даних (ETL), зберігає їх у реляційній базі даних та надає зручний інтерфейс для аналітики.

## 🚀 Функціонал

1. **ETL Процес:** Завантаження актуальних даних про країни через **RestCountries API** та збереження їх у базу даних **PostgreSQL**.  
2. **Візуалізація:** Інтерактивна карта світу (Choropleth map) побудована на даних з БД.  
3. **SQL Аналітика:** Агрегація статистики по регіонах (сумарне населення, найбільша/найменша країна) виконується **одним складним SQL запитом**.  
4. **Історичні дані:** Побудова графіків зміни населення за 1990–2023 роки (через **World Bank API**).  
5. **Контейнеризація:** Повне розгортання через **Docker Compose**.

---

## 🛠 Технічний стек

- **Python 3.10**  
- **Streamlit** (UI та логіка додатку)  
- **PostgreSQL 15** (Зберігання даних)  
- **Docker & Docker Compose** (Оркестрація)  
- **Plotly Express** (Графіки та карти)  
- **Pandas & Psycopg2** (Обробка даних та робота з БД)

---

## 📂 Структура проекту

```text
population_dashboard/
├── docker-compose.yml   # Налаштування сервісів (db + app)
├── Dockerfile           # Інструкція збірки Python-додатку
├── requirements.txt     # Залежності Python
├── app.py               # Основний код додатку (Streamlit)
└── README.md            # Документація
````

---

## ⚙️ Як запустити

Переконайтеся, що у вас встановлено **Docker** та **Docker Compose**.

### 1. Клонування репозиторію

```bash
git clone https://github.com/WolfDevOpsDev/population_stat
cd population_stat
```

### 2. Запуск проєкту

```bash
docker-compose up --build
```

Ця команда завантажить образи **PostgreSQL** та **Python**, встановить залежності та запустить сервіси.

### 3. Відкрити у браузері

```
http://localhost:8501
```

